const  {generateFakeMusicData} = require("./src/music.js")

describe("generateFakeMusicData()", () => {
    test("it generates fake music data", () => {
    const fakeMusic = generateFakeMusicData()
    expect()
    }) 
})